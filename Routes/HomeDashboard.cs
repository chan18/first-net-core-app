using System;
using Microsoft.AspNetCore.Routing;

namespace Home {
    class RoutesDashboard {
        public void injectRoute(IRouteBuilder routes) {
            // routes.MapRoute(name: "default",template: "{controller=Home}/{action=Index}/{id?}");
        }
    }
}